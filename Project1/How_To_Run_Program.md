# Bootloader

How to run the simulator:

	1.Navigate to the src directory in the Terminal
	2.Run the following command to compile the bootloader and createimage and create a 		  bootable image
		make
	3.Run the following command to run the Bochs emulator:
		bochs -q
